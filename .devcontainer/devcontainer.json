{
  "name": "GoodMem DevContainer",
  "build": { "dockerfile": "Dockerfile" },
  "containerEnv": {
    "GITHUB_TOKEN": "${localEnv:GITHUB_TOKEN}"
  },
  "postCreateCommand": "bash -lc 'set -e;\n echo Running GoodMem installer (server/DB) &&\n curl -s https://get.goodmem.ai | bash -s -- --debug-install || true;\n echo Installing GoodMem CLI via GitHub CLI &&\n sudo apt-get update && sudo apt-get install -y gh;\n printf \"%s\" \"$GITHUB_TOKEN\" | gh auth login --hostname github.com --with-token;\n gh release download -R PAIR-Systems-Inc/goodmem --pattern \"*linux*amd64*.tar.gz\" --clobber -D /tmp;\n sudo tar -xzf /tmp/*.tar.gz -C /usr/local/bin;\n sudo chmod +x /usr/local/bin/goodmem;\n goodmem --version || true;\n printf \"OPENAI_API_KEY=your_openai_api_key_here\\nADD_API_KEY=your_goodmem_api_key_here\\n\" > .env;\n echo \".env file created - GoodMem server should be running on localhost:8080\"'",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-vscode.vscode-docker"
      ]
    }
  },
  "mounts": [
    "source=goodmem-data,target=/var/lib/goodmem,type=volume"
  ],
  "remoteUser": "vscode",
  "runArgs": ["--privileged"]
}
